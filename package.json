{
  "name": "courses-api",
  "version": "1.0.0",
  "description": "API for courses using json-server",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
